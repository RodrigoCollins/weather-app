type Colors = {
  primary: string;
  background: string;
  text_dark: string;
  text_light: string;
  error: string;
  success: string;
  disabled: string;
};

const colors: Colors = {
  primary: "#1976d2",
  background: "#FFFFFF",
  text_light: "#FFFFFF",
  text_dark: "#202020",
  error: "#FF0000",
  success: "#00C853",
  disabled: "#B0B0B0",
};

export default colors;
