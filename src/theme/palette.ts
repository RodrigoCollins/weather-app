import colors from "./colors";

const palette = {
  type: "dark",
  primary: {
    main: colors.primary,
  },
  secondary: {
    main: colors.primary,
  },
  background: {
    default: colors.background,
  },
  text: {
    primary: colors.text_dark,
    secondary: colors.text_dark,
  },
  error: {
    main: colors.error,
  },
  success: {
    main: colors.success,
  },
  action: {
    disabled: colors.disabled,
  },
};

export default palette;
